#include "main.h"

/**
 * _islower - program checks for lowercase character
 * @c: the charaacter to check
 * Return: 1 if c lowercase, and 0 if its not
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
